package Toys;

public class ModelCars extends Toy {
    public ModelCars(int id, int quantity, int weight) {
        super.id = id;
        super.name = "Model Car";
        super.quantity = quantity;
        super.weight = weight;
    }
}

